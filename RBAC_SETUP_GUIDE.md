# Система управления пользователями с ролями - Инструкция

## 📋 Обзор изменений

Реализована полноценная система управления пользователями с разделением ролей:

- **Admin** - полный доступ ко всем функциям
- **User** - только просмотр контактов (read-only)

## 🔐 Роли и права доступа

### Admin (Администратор)

✅ Просмотр всех контактов
✅ Создание новых контактов
✅ Редактирование контактов
✅ Удаление контактов
✅ Экспорт в XML
✅ Управление пользователями (Settings)
✅ Создание новых пользователей
✅ Удаление пользователей
✅ Изменение ролей пользователей

### User (Пользователь)

✅ Просмотр всех контактов
✅ Фильтрация и сортировка
❌ Создание контактов
❌ Редактирование контактов
❌ Удаление контактов
❌ Экспорт в XML
❌ Доступ к Settings

## 🚀 Первый запуск

### 1. Установка зависимостей backend

```bash
cd backend
npm install
```

Новая зависимость: `jsonwebtoken`

### 2. Настройка переменных окружения (опционально)

Создайте файл `.env` в папке `backend/`:

```env
# JWT секретный ключ (рекомендуется изменить в продакшене!)
JWT_SECRET=your-super-secret-key-change-this

# Время жизни токена (по умолчанию 24 часа)
JWT_EXPIRES_IN=24h

# Данные администратора по умолчанию
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
```

### 3. Запуск backend

```bash
cd backend
npm run server
```

При первом запуске автоматически создастся пользователь **admin** с паролем **admin**.

⚠️ **ВАЖНО**: Смените пароль после первого входа!

### 4. Запуск frontend

```bash
cd frontend
npm run dev
```

## 📝 Использование

### Вход в систему

1. Откройте приложение в браузере
2. Введите логин: `admin`
3. Введите пароль: `admin`
4. Нажмите "Log in"

### Создание нового пользователя

1. Войдите как администратор
2. Нажмите на иконку ⚙️ Settings в шапке
3. В разделе "Add a new user..." введите:
   - Username (имя пользователя)
   - Password (пароль)
   - Role (роль): User или Admin
4. Нажмите "Create or update"

### Просмотр списка пользователей

1. Откройте Settings
2. Нажмите кнопку "Show" в разделе "Show all users"
3. Увидите список всех пользователей с их ролями

### Удаление пользователя

1. Откройте Settings
2. В разделе "Delete user" введите имя пользователя
3. Нажмите "Delete"

## 🔧 Технические детали

### Backend изменения

#### Новые файлы:

- `backend/middleware/auth.middleware.js` - middleware для JWT аутентификации

#### Измененные файлы:

- `backend/models/authentication.model.js` - добавлено поле `role`
- `backend/routes/auth.routes.js` - добавлена выдача JWT токенов
- `backend/routes/user.routes.js` - защита роутов, поддержка ролей
- `backend/routes/phones.routes.js` - защита операций записи
- `backend/userUtils.js` - автоматическое создание admin
- `backend/server.js` - инициализация admin при старте

### Frontend изменения

#### Новые файлы:

- `frontend/src/contexts/AuthContext.jsx` - контекст для управления аутентификацией

#### Измененные файлы:

- `frontend/src/App.jsx` - интеграция AuthProvider
- `frontend/src/components/LoginForm.jsx` - работа с JWT
- `frontend/src/components/Header.jsx` - скрытие Settings и Converter для User
- `frontend/src/components/Contacts.jsx` - скрытие кнопок редактирования/удаления
- `frontend/src/components/Settings.jsx` - выбор роли при создании пользователя
- `frontend/public/locales/*.json` - новые переводы

### API защита

Все защищенные роуты требуют JWT токен в заголовке:

```
Authorization: Bearer <token>
```

#### Защищенные эндпоинты:

**Доступны всем авторизованным пользователям:**

- `GET /api/contacts/get-all` - просмотр контактов

**Только для admin:**

- `POST /api/contacts/add-contact` - создание контакта
- `PUT /api/contacts/edit-contact/:id` - редактирование контакта
- `DELETE /api/contacts/delete-contact/:id` - удаление контакта
- `POST /api/user/createOrUpdateUser` - создание/обновление пользователя
- `DELETE /api/user/deleteUser/:username` - удаление пользователя
- `GET /api/user/getAllUserLogins` - список пользователей

## 🔒 Безопасность

### Рекомендации для продакшена:

1. **Измените JWT_SECRET** в переменных окружения на случайную строку
2. **Смените пароль admin** сразу после первого входа
3. **Используйте HTTPS** для всех запросов
4. **Настройте CORS** для разрешенных доменов
5. **Регулярно обновляйте** зависимости

### Генерация надежного JWT_SECRET:

```bash
# В терминале (Node.js)
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

## 🐛 Возможные проблемы и решения

### Ошибка "Invalid or expired token"

- Токен истек (24 часа по умолчанию)
- Решение: Выйдите и войдите заново

### Ошибка "Access denied. Required role: admin"

- Недостаточно прав для выполнения операции
- Решение: Войдите под учетной записью администратора

### База данных пустая после обновления

- Поле `role` было добавлено в модель
- Решение: Проверьте, что база данных обновилась (Sequelize автоматически добавит колонку)

### Не виден admin при первом запуске

- Проверьте логи сервера на наличие сообщения "Admin user created successfully"
- Если нет, удалите старые записи из таблицы `authentication` и перезапустите сервер

## 📊 Структура таблицы Users

```sql
CREATE TABLE authentications (
  id INT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(255) UNIQUE NOT NULL,
  hashedPassword VARCHAR(255) NOT NULL,
  role ENUM('admin', 'user') DEFAULT 'user' NOT NULL,
  createdAt DATETIME,
  updatedAt DATETIME
);
```

## 🎯 Дальнейшие улучшения (опционально)

- Аудит лог действий пользователей
- Email уведомления при создании/удалении пользователей
- Восстановление пароля
- Двухфакторная аутентификация (2FA)
- Ограничение попыток входа
- История изменений контактов
- Профиль пользователя (смена своего пароля)
- Refresh tokens для автоматического обновления

## 📞 Поддержка

При возникновении вопросов или проблем:

1. Проверьте логи backend сервера
2. Проверьте консоль браузера на наличие ошибок
3. Убедитесь, что все зависимости установлены
4. Проверьте, что backend и frontend запущены

---

**Версия**: 2.0.0  
**Дата обновления**: Январь 2026  
**Автор**: Denis Nazarow
