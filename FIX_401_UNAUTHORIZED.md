# ✅ Исправление ошибки 401 (Unauthorized)

## 🔧 Что было исправлено:

1. **Создан axiosInstance** - новый axios instance с автоматическим добавлением JWT токена
2. **Обновлены все компоненты** - теперь используют axiosInstance вместо обычного axios
3. **Request Interceptor** - автоматически добавляет токен из localStorage к каждому запросу

## 📋 Измененные файлы:

### Новые файлы:

- `frontend/src/utils/axiosInstance.js` - axios instance с interceptor

### Обновленные файлы:

- `frontend/src/services/paramsAPI.js` - использует axiosInstance
- `frontend/src/components/LoginForm.jsx` - использует axiosInstance
- `frontend/src/components/Settings.jsx` - использует axiosInstance
- `frontend/src/components/Converter.jsx` - использует axiosInstance

## 🚀 Как проверить:

1. **Очистите localStorage** (если есть старые данные):

   - F12 → Application → Local Storage → Удалите все ключи

2. **Перезагрузите страницу** (F5 или Ctrl+R)

3. **Войдите в систему**:

   - Логин: `admin`
   - Пароль: `admin`

4. **Проверьте консоль** - больше не должно быть ошибок 401

5. **Проверьте контакты** - должны загрузиться

## 🔍 Как работает:

```javascript
// При каждом запросе через axiosInstance:
const token = localStorage.getItem('token');
if (token) {
  config.headers.Authorization = `Bearer ${token}`;
}
```

Токен автоматически добавляется в заголовок Authorization для всех запросов!

## ⚠️ Важно:

После входа токен сохраняется в localStorage и автоматически используется для всех последующих запросов.

---

**Статус**: ✅ Готово к тестированию
