# 🚀 Первый запуск PhoneBook

Руководство по первому запуску проекта на новом компьютере.

## 📋 Требования

- **Node.js** >= 16.x
- **MySQL** >= 8.0
- **npm** или **yarn**

## 🔧 Шаг 1: Клонирование проекта

```bash
git clone https://github.com/Denis-Hish/PhoneBook.git
cd PhoneBook
```

## 💾 Шаг 2: Настройка базы данных

### 2.1 Создайте базу данных MySQL

```sql
CREATE DATABASE phonebook CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2.2 Создайте файл `.env` в папке `backend/`

```bash
cd backend
```

Создайте файл `.env` со следующим содержимым:

```env
# Настройки базы данных
DB_HOST='localhost'          # или имя вашего MySQL сервера
DB_USER='root'               # имя пользователя MySQL
DB_PASSWORD='your_password'  # пароль MySQL (оставьте пустым если нет пароля)
DB_NAME='phonebook'          # имя базы данных

# JWT секретный ключ (ОБЯЗАТЕЛЬНО СМЕНИТЕ В ПРОДАКШЕНЕ!)
JWT_SECRET='your-super-secret-jwt-key-change-this'
JWT_EXPIRES_IN='24h'

# Учетные данные администратора по умолчанию
ADMIN_USERNAME='admin'
ADMIN_PASSWORD='admin'
```

### 2.3 Генерация JWT_SECRET (рекомендуется)

```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

Скопируйте полученную строку и вставьте в `.env` как значение `JWT_SECRET`.

## 📦 Шаг 3: Установка зависимостей

### 3.1 Установка зависимостей backend

```bash
cd backend
npm install
```

### 3.2 Установка зависимостей frontend

```bash
cd ../frontend
npm install
```

### 3.3 Установка корневых зависимостей (опционально)

Если хотите использовать команду `npm run dev` из корня проекта:

```bash
cd ..
npm install
```

## 🗄️ Шаг 4: Инициализация базы данных

При первом запуске backend сервера:

- Разкомментируйте строку createOrUpdateAdminUser... в userUtils.js, после того, как в базе данных будет создан пользователь admin, строку можно обратно закомментировать
- Sequelize автоматически создаст необходимые таблицы
- Будет создан администратор с учетными данными из `.env` или, если в .env данные не указаны, будет создан пользователь `admin` с паролем `admin`

**Важно:** Если у вас уже была старая версия БД без колонки `role`, выполните миграцию:

```bash
cd backend
npm run db:add-role
```

## ▶️ Шаг 5: Запуск приложения

### Вариант А: Запуск из корня проекта (рекомендуется)

```bash
npm run dev
```

Эта команда одновременно запустит:

- Backend сервер на http://localhost:8080
- Frontend dev сервер на http://localhost:5173

### Вариант Б: Запуск раздельно

**Терминал 1 - Backend:**

```bash
cd backend
npm run server
```

**Терминал 2 - Frontend:**

```bash
cd frontend
npm run dev
```

## 🔐 Шаг 6: Маршруты приложения

После запуска доступны следующие маршруты (НОВОЕ в v2.1!):

- **`http://localhost:5173/login`** - страница авторизации (публичная)
- **`http://localhost:5173/`** - главная страница (защищённая, требует авторизацию)

**Автоматические редиректы:**

- Неавторизованный пользователь → автоматически на `/login`
- Авторизованный на `/login` → автоматически на `/`

## 🔐 Шаг 7: Первый вход

1. Откройте браузер: http://localhost:5173 (автоматически перенаправит на `/login`)
2. Введите учетные данные:
   - **Username**: `admin`
   - **Password**: `admin`
3. Нажмите **Log in**

После успешного входа вы попадёте на главную страницу с контактами.

⚠️ **ВАЖНО**: Смените пароль администратора после первого входа в Settings!

## ✅ Проверка работоспособности

После успешного запуска вы должны видеть:

### Backend логи:

```
Server is running on port 8080.
Connected to the database!
✅ Admin user created successfully
```

### Frontend:

- Страница авторизации загружается на `/login`
- Header на странице логина показывает переключение темы и языка
- После входа будет переадресация на главную страницу `/` будет видна таблица контактов с надписью: "Здесь пока пусто. Создайте первую запись."
- Добавьте первый контакт
- Доступны все функции (Settings, Converter, редактирование) для admin роли
- Для user роли видны только контакты без возможности редактирования

## 🎯 Создание пользователей

### Создание обычного пользователя (read-only):

1. Войдите как администратор
2. Нажмите иконку ⚙️ **Settings** в шапке
3. В разделе "Add a new user...":
   - **Username**: имя пользователя (обязательно)
   - **Password**: пароль (желательно, но не обязательно)
   - **Role**: выберите `User (read_only)` из выпадающего списка (обязательно)
4. Нажмите **Create or update**

### Создание администратора:

1. Повторите шаги 1-2
2. В разделе "Add a new user...":
   - **Username**: имя пользователя (обязательно)
   - **Password**: пароль (желательно, но не обязательно)
   - **Role**: выберите `Admin (full_access)` из выпадающего списка (обязательно)
3. Нажмите **Create or update**

## 🐛 Решение проблем

### Ошибка подключения к БД

```
Cannot connect to the database!
```

**Решение:**

- Проверьте, что MySQL сервер запущен
- Проверьте данные в `.env` (DB_HOST, DB_USER, DB_PASSWORD, DB_NAME)
- Убедитесь, что база данных `phonebook` создана

### Ошибка "Unknown column 'role'"

```
Error: Unknown column 'role' in 'field list'
```

**Решение:**

```bash
cd backend
npm run db:add-role
```

### Ошибка 500 при авторизации

**Решение:**

1. Проверьте логи backend сервера
2. Убедитесь, что таблица `authentications` имеет колонку `role`
3. Выполните миграцию (см. выше)
4. Перезапустите сервер

### Ошибка 401 (Unauthorized)

**Решение:**

1. Очистите localStorage браузера: F12 → Application → Local Storage → Clear All
2. Перезагрузите страницу
3. Войдите заново

### Frontend не подключается к Backend

**Решение:**

- Убедитесь, что backend запущен на порту 8080
- Проверьте `frontend/vite.config.js` - должен быть настроен proxy на `http://localhost:8080`

## 📚 Дополнительные ресурсы

После успешного запуска ознакомьтесь с:

- [README.md](README.md) - описание проекта и возможностей
- [CHANGELOG.md](CHANGELOG.md) - история изменений (v2.1.0)

## 🔒 Безопасность (для продакшена)

Перед развертыванием на сервере:

1. ✅ Смените `JWT_SECRET` на случайную строку (64+ символов)
2. ✅ Смените пароль `admin`
3. ✅ Используйте HTTPS
4. ✅ Настройте CORS для конкретных доменов
5. ✅ Используйте сильные пароли для MySQL
6. ✅ Регулярно обновляйте зависимости
7. ✅ Настройте Content Security Policy (CSP) на уровне сервера
8. ✅ Используйте переменные окружения для всех чувствительных данных

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи backend в терминале
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Убедитесь, что все зависимости установлены
4. Проверьте, что порты 8080 и 5173 не заняты другими приложениями
5. Проверьте права доступа на папку проекта

---

**Версия**: 2.1.0  
**Автор**: Denis Nazarow
**Дата**: Январь 2026
