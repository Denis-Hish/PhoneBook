# 📞 PhoneBook - Система управления контактами

Веб-приложение для управления телефонной книгой с системой ролей и экспортом в XML.

## 📖 Описание

**PhoneBook** - это современное веб-приложение для управления контактами, разработанное специально для организаций, использующих IP-телефоны **Yealink**. Приложение позволяет централизованно управлять телефонной книгой и автоматически экспортировать контакты в формат XML для загрузки на телефонные аппараты.

### Для чего создавалось

Приложение решает следующие задачи:

- 📋 Централизованное хранение всех контактов организации
- 👥 Управление доступом через систему ролей (администраторы и пользователи)
- 📱 Автоматический экспорт контактов в формат Yealink XML
- 🔍 Быстрый поиск и фильтрация контактов
- 🌍 Поддержка международных телефонных кодов
- 🌐 Многоязычный интерфейс (английский, польский, украинский)

## ✨ Основные возможности

### Для администраторов (admin):

- ✅ Просмотр всех контактов
- ✅ Создание, редактирование и удаление контактов
- ✅ Экспорт телефонной книги в XML формат (Yealink)
- ✅ Управление пользователями (создание, удаление)
- ✅ Назначение ролей пользователям
- ✅ Полный доступ к настройкам системы

### Для пользователей (user):

- ✅ Просмотр всех контактов
- ✅ Фильтрация и сортировка контактов
- ✅ Поиск по имени и телефону
- ❌ Без возможности редактирования и удаления

### Дополнительные функции:

- 🎨 Переключение темы (светлая/тёмная)
- 🌐 Смена языка интерфейса
- 📊 Просмотр общего количества контактов
- 🔒 Безопасная аутентификация через JWT
- 📱 Адаптивный дизайн для мобильных устройств

## 🛠️ Технологии

### Backend:

- **Node.js** + **Express.js** - серверная часть
- **MySQL** + **Sequelize** - база данных и ORM
- **JWT** - аутентификация и авторизация
- **Argon2** - безопасное хеширование паролей
- **Helmet** + **CORS** - безопасность

### Frontend:

- **React** 18 + **Vite** - интерфейс
- **Material-UI** (MUI) - компоненты UI
- **Axios** - HTTP запросы
- **i18next** - интернационализация
- **React Router** - маршрутизация

## 📦 Структура проекта

```
PhoneBook/
├── backend/                  # Серверная часть
│   ├── config/              # Конфигурация БД
│   ├── middleware/          # Middleware (auth)
│   ├── models/              # Модели Sequelize
│   ├── routes/              # API маршруты
│   ├── xml/                 # Сгенерированные XML файлы
│   ├── server.js            # Главный файл сервера
│   ├── userUtils.js         # Утилиты для работы с пользователями
│   ├── scripts/             # Разовые скрипты БД
│   │   ├── check-db.js      # Проверка подключения и пользователей
│   │   ├── migrate-db.js    # Обновление схемы (alter)
│   │   └── migrate-role.js  # Точечная миграция: колонка role
│   └── package.json
├── frontend/                 # Клиентская часть
│   ├── public/
│   │   └── locales/         # Файлы переводов
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── contexts/        # React контексты (Auth)
│   │   ├── services/        # API сервисы
│   │   ├── utils/           # Утилиты (axios instance)
│   │   └── App.jsx          # Главный компонент
│   └── package.json
└── package.json              # Корневой package.json
```

## 🚀 Быстрый старт

### Требования:

- Node.js >= 16.x
- MySQL >= 8.0
- npm или yarn

### Установка и запуск:

1. **Клонирование проекта:**

   ```bash
   git clone https://github.com/Denis-Hish/PhoneBook.git
   cd PhoneBook
   ```

2. **Настройка базы данных:**

   ```sql
   CREATE DATABASE phonebook;
   ```

3. **Настройка переменных окружения:**

   Создайте файл `backend/.env`:

   ```env
   DB_HOST='localhost'
   DB_USER='root'
   DB_PASSWORD=''
   DB_NAME='phonebook'

   JWT_SECRET='ваш-секретный-ключ'
   JWT_EXPIRES_IN='24h'

   ADMIN_USERNAME='admin'
   ADMIN_PASSWORD='admin'
   ```

4. **Установка зависимостей и запуск:**

   ```bash
   # Установка всех зависимостей
   npm install
   cd backend && npm install
   cd ../frontend && npm install

   # Запуск (из корня проекта)
   cd ..
   npm run dev
   ```

5. **Откройте браузер:**

   - Frontend: http://localhost:5173
   - Backend API: http://localhost:8080

   Войдите с учетными данными: `admin` / `admin`

📖 **Подробная инструкция**: См. [FIRST_RUN.md](FIRST_RUN.md)

## 🔐 Система ролей

Приложение поддерживает две роли:

| Роль  | Доступ                                |
| ----- | ------------------------------------- |
| admin | Полный доступ ко всем функциям        |
| user  | Только просмотр контактов (read-only) |

## 📱 Экспорт в XML (Yealink)

Администраторы могут экспортировать телефонную книгу в два формата:

1. **PhoneBook_1_Yealink.xml** - полный формат с офисными и мобильными номерами
2. **PhoneBook_2.xml** - упрощенный формат только с мобильными номерами

Файлы сохраняются в `backend/xml/` и могут быть загружены на IP-телефоны Yealink через веб-интерфейс аппарата.

## 🌐 Поддерживаемые языки

- 🇬🇧 Английский (English)
- 🇵🇱 Польский (Polski)
- 🇺🇦 Украинский (Українська)

## 📄 API Endpoints

### Аутентификация:

- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получение информации о текущем пользователе

### Контакты:

- `GET /api/contacts/get-all` - Получение всех контактов
- `POST /api/contacts/add-contact` - Добавление контакта (admin)
- `PUT /api/contacts/edit-contact/:id` - Редактирование контакта (admin)
- `DELETE /api/contacts/delete-contact/:id` - Удаление контакта (admin)

### Пользователи:

- `POST /api/user/createOrUpdateUser` - Создание/обновление пользователя (admin)
- `GET /api/user/getAllUserLogins` - Список всех пользователей (admin)
- `DELETE /api/user/deleteUser/:username` - Удаление пользователя (admin)

### XML:

- `POST /create-xml-files` - Генерация XML файлов (admin)

## 🔒 Безопасность

- JWT токены с временем жизни 24 часа
- Пароли хешируются через Argon2
- Защита всех API endpoints через middleware
- CORS настроен для разрешенных доменов
- Rate limiting для защиты от DDoS
- Helmet для дополнительной защиты заголовков

## 📚 Документация

- [FIRST_RUN.md](FIRST_RUN.md) - Инструкция по первому запуску
- [CHANGELOG.md](CHANGELOG.md) - История изменений

## 🤝 Вклад в проект

Проект находится в активной разработке. Приветствуются:

- Сообщения об ошибках
- Предложения по улучшению
- Pull requests

## 📝 Лицензия

ISC License

## 👤 Автор

**Denis Nazarow**

- GitHub: [@Denis-Hish](https://github.com/Denis-Hish)
- Email: [Указать email]

## 🙏 Благодарности

Проект создан для организации [Nadzieja Rodzinie](https://nadziejarodzinie.org.pl/)

---

**Версия**: 2.0.0  
**Последнее обновление**: Январь 2026
