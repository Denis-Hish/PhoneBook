# 📝 Changelog - Система управления пользователями v2.0

## Дата: Январь 2026

### 🎯 Основные изменения

#### ✨ Новые функции

1. **Система ролей (RBAC)**

   - Роль `admin` - полный доступ
   - Роль `user` - только чтение

2. **JWT аутентификация**

   - Безопасная передача токенов
   - Автоматическая авторизация при каждом запросе
   - Проверка срока действия токена (24 часа по умолчанию)

3. **Автоматическая инициализация**

   - Создание admin пользователя при первом запуске
   - Настройка через переменные окружения

4. **Улучшенный UI**
   - Скрытие кнопок в зависимости от роли
   - Индикация роли при создании пользователей
   - Отображение ролей в списке пользователей

#### 🔐 Безопасность

- JWT токены вместо простой авторизации
- Middleware для проверки прав доступа
- Защита API endpoints по ролям
- Хеширование паролей через argon2

---

## 🗂️ Измененные файлы

### Backend

#### Новые файлы:

```
backend/
  └── middleware/
      └── auth.middleware.js          # JWT middleware
```

#### Измененные файлы:

```
backend/
  ├── models/
  │   └── authentication.model.js     # + поле role
  ├── routes/
  │   ├── auth.routes.js              # + JWT токены, /me endpoint
  │   ├── user.routes.js              # + защита роутов, поддержка ролей
  │   └── phones.routes.js            # + защита операций записи
  ├── server.js                       # + автоинициализация admin
  ├── userUtils.js                    # + initializeAdminUser()
  ├── package.json                    # + jsonwebtoken
  └── .env.example                    # + JWT настройки
```

### Frontend

#### Новые файлы:

```
frontend/
  └── src/
      └── contexts/
          └── AuthContext.jsx         # Контекст аутентификации
```

#### Измененные файлы:

```
frontend/
  ├── src/
  │   ├── App.jsx                     # + AuthProvider
  │   └── components/
  │       ├── LoginForm.jsx           # + работа с JWT
  │       ├── Header.jsx              # + скрытие Settings/Converter
  │       ├── Contacts.jsx            # + скрытие кнопок edit/delete
  │       └── Settings.jsx            # + выбор роли
  └── public/
      └── locales/
          ├── en.json                 # + переводы для ролей
          ├── pl.json                 # + переводы для ролей
          └── ua.json                 # + переводы для ролей
```

### Документация

#### Новые файлы:

```
/
├── RBAC_SETUP_GUIDE.md              # Полная инструкция
└── MIGRATION_GUIDE.md               # Руководство по миграции
```

---

## 🔄 API изменения

### Новые endpoints:

**POST** `/api/auth/login`

```json
Request:
{
  "username": "admin",
  "password": "admin"
}

Response:
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1Ni...",
  "user": {
    "username": "admin",
    "role": "admin"
  }
}
```

**GET** `/api/auth/me` (требует токен)

```json
Response:
{
  "success": true,
  "user": {
    "username": "admin",
    "role": "admin"
  }
}
```

### Измененные endpoints:

**POST** `/api/user/createOrUpdateUser` (теперь требует admin роль)

```json
Request:
{
  "username": "john",
  "password": "password123",
  "role": "user"  // новое поле
}
```

**GET** `/api/user/getAllUserLogins` (теперь требует admin роль)

```json
Response:
{
  "users": [
    { "username": "admin", "role": "admin" },
    { "username": "john", "role": "user" }
  ]
}
```

**Все контактные endpoints** теперь требуют токен:

- `GET /api/contacts/get-all` - доступно всем
- `POST /api/contacts/add-contact` - только admin
- `PUT /api/contacts/edit-contact/:id` - только admin
- `DELETE /api/contacts/delete-contact/:id` - только admin

---

## 📊 Статистика изменений

- **Новых файлов**: 4
- **Измененных файлов**: 15
- **Добавлено строк кода**: ~600
- **Новых зависимостей**: 1 (jsonwebtoken)
- **Новых endpoints**: 2
- **Защищенных endpoints**: 7

---

## 🚀 Следующие шаги

1. Протестируйте систему с разными ролями
2. Смените пароль admin в продакшене
3. Настройте JWT_SECRET через переменные окружения
4. Создайте резервную копию базы данных
5. Обучите пользователей новой системе

---

## 📚 Дополнительные ресурсы

- [RBAC_SETUP_GUIDE.md](./RBAC_SETUP_GUIDE.md) - Полная инструкция
- [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md) - Миграция существующего проекта
- [JWT Documentation](https://jwt.io/) - Официальная документация JWT

---

## 🤝 Поддержка

Если возникли вопросы:

1. Проверьте [RBAC_SETUP_GUIDE.md](./RBAC_SETUP_GUIDE.md)
2. Проверьте [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md)
3. Проверьте логи backend сервера
4. Проверьте консоль браузера

---

**Версия**: 2.0.0  
**Обратная совместимость**: Требуется миграция  
**Тип релиза**: Major (Breaking Changes)
