# server
PORT='8080'
FRONTEND_ORIGIN='https://phonebook.com'

# mySQL
DB_HOST=''
DB_USER=''
DB_PASSWORD=''
DB_NAME='phonebook'

# JWT Configuration
# ВАЖНО: Измените эти значения в продакшене!
# Секретный ключ для подписи JWT токенов
# Для генерации: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET='your-super-secret-key-change-this-in-production'

# Время жизни токена (примеры: 1h, 24h, 7d, 30d)
JWT_EXPIRES_IN='24h'

# Данные администратора по умолчанию (используются при первом запуске)
ADMIN_USERNAME='admin'
ADMIN_PASSWORD='admin'