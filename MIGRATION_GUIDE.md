# 🔄 Миграция на систему ролей - Краткая инструкция

## ⚡ Быстрый старт для существующего проекта

### 1. Остановите сервер

```bash
# Нажмите Ctrl+C в терминале где запущен backend
```

### 2. Установите новые зависимости

```bash
cd backend
npm install jsonwebtoken
```

### 3. Обновите базу данных

**Автоматически**: Sequelize добавит колонку `role` при следующем запуске

**Вручную (MySQL)**: Если хотите контролировать процесс:

```sql
USE phonebook;
ALTER TABLE authentications ADD COLUMN role ENUM('admin', 'user') DEFAULT 'user' NOT NULL AFTER hashedPassword;
```

### 4. Опционально: Настройте переменные окружения

Скопируйте `.env.example` в `.env` и измените значения:

```bash
cd backend
cp .env.example .env
```

Отредактируйте `.env`:

```env
JWT_SECRET=ваш-случайный-секретный-ключ
JWT_EXPIRES_IN=24h
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
```

### 5. Обновите существующих пользователей (если есть)

**Вариант A**: Через SQL

```sql
-- Сделать конкретного пользователя администратором
UPDATE authentications SET role = 'admin' WHERE username = 'admin';

-- Сделать всех существующих пользователей администраторами
UPDATE authentications SET role = 'admin';

-- Сделать всех обычными пользователями (кроме admin)
UPDATE authentications SET role = 'user' WHERE username != 'admin';
```

**Вариант B**: Через код
Раскомментируйте и выполните в `userUtils.js`:

```javascript
// Обновить существующего пользователя
createOrUpdateAdminUser('admin', 'admin', 'admin');
```

### 6. Запустите backend

```bash
cd backend
npm run server
```

Вы должны увидеть:

```
✅ Database already contains users
```

или

```
✅ Admin user created successfully
```

### 7. Очистите браузер

**Важно!** Удалите старые данные авторизации:

1. Откройте DevTools (F12)
2. Application → Local Storage
3. Удалите ключи: `authData`, `token`
4. Обновите страницу (F5)

### 8. Войдите заново

Логин: `admin`  
Пароль: `admin` (или тот, что вы установили)

### 9. Создайте тестового пользователя

1. Откройте Settings (⚙️)
2. Создайте нового пользователя с ролью `user`
3. Выйдите и войдите под этим пользователем
4. Убедитесь, что кнопки редактирования скрыты

## ✅ Проверка работоспособности

### Как admin:

- ✅ Видна кнопка Settings
- ✅ Видна кнопка Convert to XML
- ✅ Видны кнопки редактирования и удаления контактов
- ✅ Виден список всех пользователей
- ✅ Можно создавать/удалять пользователей

### Как user:

- ❌ НЕ видна кнопка Settings
- ❌ НЕ видна кнопка Convert to XML
- ❌ НЕ видны кнопки редактирования и удаления контактов
- ✅ Виден список контактов
- ✅ Работает фильтрация и сортировка

## 🐛 Решение проблем

### "Cannot read property 'role' of null"

**Причина**: Старые пользователи без роли  
**Решение**: Обновите роли через SQL (см. пункт 5)

### "Invalid or expired token"

**Причина**: Старый токен в localStorage  
**Решение**: Очистите localStorage и войдите заново

### Не работают API запросы

**Причина**: Отсутствует токен в заголовках  
**Решение**: Перезапустите backend и frontend, войдите заново

### Ошибка "role is not defined"

**Причина**: База данных не обновилась  
**Решение**: Добавьте колонку `role` вручную (см. пункт 3)

## 📋 Контрольный список

- [ ] Backend зависимости установлены
- [ ] База данных обновлена (колонка `role` добавлена)
- [ ] Backend запущен без ошибок
- [ ] Frontend запущен без ошибок
- [ ] localStorage очищен
- [ ] Вход как admin работает
- [ ] Settings доступны для admin
- [ ] Создан тестовый user
- [ ] Вход как user работает
- [ ] У user скрыты кнопки редактирования

## 🔄 Откат изменений (если что-то пошло не так)

1. Остановите сервер
2. Откатите изменения в Git:
   ```bash
   git checkout main
   ```
3. Восстановите базу данных из бэкапа
4. Запустите сервер

---

**Время миграции**: ~10 минут  
**Требуется перезагрузка**: Да (backend и frontend)  
**Требуется очистка кэша**: Да (localStorage в браузере)
